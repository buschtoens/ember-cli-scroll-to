import { scrollTo } from '../../../helpers/scroll-to';
import { module, test } from 'qunit';

module('Unit | Helper | scroll to');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = scrollTo(42);
  assert.ok(result);
});
